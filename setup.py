from setuptools import find_packages, setup

"""
"""
setup(
    name="savanna",  
    version="1.0.0",
    author="Michael Poli",
    packages=find_packages(), 
    install_requires=[
    ],
    entry_points={
    },
)